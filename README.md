# homebrew-tap

Homebrew tap for mahope tools.

## Install deskuptime

```bash
brew install mahope/tap/deskuptime
```

Website uptime monitor CLI — uptime, SSL expiry & content change alerts from your terminal or CI. Zero dependencies (Node runtime required).

## Install clean-copy

```bash
brew install mahope/tap/clean-copy
```
